#!/bin/sh
# scripts/p8_test.sh -- PLACEHOLDER written by SOP step W1.14 (the Makefile).
#
# Owner: SOP step W8.x, fleet phase 14. That step replaces this file wholesale
# and never edits the Makefile: the target `make p8-test` already
# delegates here.
#
# ABSUMP_PLACEHOLDER. The existence of this file is not evidence that
# W8.x has run. It prints one line and exits 0 so that
# `make -n p8-test` and `make p8-test` both work today.
echo "p8-test: not built yet in this phase (owner: SOP W8.x, fleet phase 14)."
exit 0
