#!/bin/sh
# scripts/test_ch1.sh -- PLACEHOLDER written by SOP step W1.14 (the Makefile).
#
# Owner: SOP step W3.x, fleet phase 08. That step replaces this file wholesale
# and never edits the Makefile: the target `make test-ch1` already
# delegates here.
#
# ABSUMP_PLACEHOLDER. The existence of this file is not evidence that
# W3.x has run. It prints one line and exits 0 so that
# `make -n test-ch1` and `make test-ch1` both work today.
echo "test-ch1: not built yet in this phase (owner: SOP W3.x, fleet phase 08)."
exit 0
