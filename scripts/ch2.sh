#!/bin/sh
# scripts/ch2.sh -- PLACEHOLDER written by SOP step W1.14 (the Makefile).
#
# Owner: SOP step W4.x, fleet phase 09. That step replaces this file wholesale
# and never edits the Makefile: the target `make ch2` already
# delegates here.
#
# ABSUMP_PLACEHOLDER. The existence of this file is not evidence that
# W4.x has run. It prints one line and exits 0 so that
# `make -n ch2` and `make ch2` both work today.
echo "ch2: not built yet in this phase (owner: SOP W4.x, fleet phase 09)."
exit 0
