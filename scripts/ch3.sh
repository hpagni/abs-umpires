#!/bin/sh
# scripts/ch3.sh -- PLACEHOLDER written by SOP step W1.14 (the Makefile).
#
# Owner: SOP step W5.x, fleet phase 10. That step replaces this file wholesale
# and never edits the Makefile: the target `make ch3` already
# delegates here.
#
# ABSUMP_PLACEHOLDER. The existence of this file is not evidence that
# W5.x has run. It prints one line and exits 0 so that
# `make -n ch3` and `make ch3` both work today.
echo "ch3: not built yet in this phase (owner: SOP W5.x, fleet phase 10)."
exit 0
