#!/bin/sh
# scripts/abstract.sh -- PLACEHOLDER written by SOP step W1.14 (the Makefile).
#
# Owner: SOP step W6.x, fleet phase 06. That step replaces this file wholesale
# and never edits the Makefile: the target `make abstract` already
# delegates here.
#
# ABSUMP_PLACEHOLDER. The existence of this file is not evidence that
# W6.x has run. It prints one line and exits 0 so that
# `make -n abstract` and `make abstract` both work today.
echo "abstract: not built yet in this phase (owner: SOP W6.x, fleet phase 06)."
exit 0
