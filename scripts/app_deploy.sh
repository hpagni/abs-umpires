#!/bin/sh
# scripts/app_deploy.sh -- PLACEHOLDER written by SOP step W1.14 (the Makefile).
#
# Owner: SOP step W7.x, fleet phase 12. That step replaces this file wholesale
# and never edits the Makefile: the target `make app-deploy` already
# delegates here.
#
# ABSUMP_PLACEHOLDER. The existence of this file is not evidence that
# W7.x has run. It prints one line and exits 0 so that
# `make -n app-deploy` and `make app-deploy` both work today.
echo "app-deploy: not built yet in this phase (owner: SOP W7.x, fleet phase 12)."
exit 0
