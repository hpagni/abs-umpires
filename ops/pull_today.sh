#!/bin/sh
# ops/pull_today.sh -- PLACEHOLDER written by SOP step W1.14 (the Makefile).
#
# Owner: SOP step W2.22, fleet phase 03. That step replaces this file wholesale
# and never edits the Makefile: the target `make pull-today` already
# delegates here.
#
# ABSUMP_PLACEHOLDER. The existence of this file is not evidence that
# W2.22 has run. It prints one line and exits 0 so that
# `make -n pull-today` and `make pull-today` both work today.
echo "pull-today: not built yet in this phase (owner: SOP W2.22, fleet phase 03)."
exit 0
