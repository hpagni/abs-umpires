#!/bin/sh
# ops/smoke.sh -- PLACEHOLDER written by SOP step W1.14 (the Makefile).
#
# Owner: SOP step W1.15, a later fleet phase. That step replaces this file wholesale
# and never edits the Makefile: the target `make smoke` already
# delegates here.
#
# ABSUMP_PLACEHOLDER. The existence of this file is not evidence that
# W1.15 has run. It prints one line and exits 0 so that
# `make -n smoke` and `make smoke` both work today.
echo "smoke: not built yet in this phase (owner: SOP W1.15, a later fleet phase)."
exit 0
