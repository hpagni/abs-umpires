#!/bin/sh
# ops/nightly.sh -- PLACEHOLDER written by SOP step W1.14 (the Makefile).
#
# Owner: SOP step W2.22, fleet phase 03. That step replaces this file wholesale
# and never edits the Makefile: the target `make nightly` already
# delegates here.
#
# ABSUMP_PLACEHOLDER. The existence of this file is not evidence that
# W2.22 has run. It prints one line and exits 0 so that
# `make -n nightly` and `make nightly` both work today.
echo "nightly: not built yet in this phase (owner: SOP W2.22, fleet phase 03)."
exit 0
