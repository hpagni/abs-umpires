#!/bin/sh
# ops/b2_check.sh -- PLACEHOLDER written by SOP step W1.14 (the Makefile).
#
# Owner: SOP step W1.10, a later fleet phase. That step replaces this file wholesale
# and never edits the Makefile: the target `make canary` already
# delegates here.
#
# ABSUMP_PLACEHOLDER. The existence of this file is not evidence that
# W1.10 has run. It prints one line and exits 0 so that
# `make -n canary` and `make canary` both work today.
echo "canary: not built yet in this phase (owner: SOP W1.10, a later fleet phase)."
exit 0
