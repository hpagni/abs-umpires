#!/bin/sh
# ops/clean_out.sh -- PLACEHOLDER written by SOP step W1.14 (the Makefile).
#
# Owner: SOP step W9.9, a later fleet phase. That step replaces this file wholesale
# and never edits the Makefile: the target `make clean-out` already
# delegates here.
#
# ABSUMP_PLACEHOLDER. The existence of this file is not evidence that
# W9.9 has run. It prints one line and exits 0 so that
# `make -n clean-out` and `make clean-out` both work today.
echo "clean-out: not built yet in this phase (owner: SOP W9.9, a later fleet phase)."
exit 0
