#!/bin/sh
# ops/lint_prose.sh -- PLACEHOLDER written by SOP step W1.14 (the Makefile).
#
# Owner: SOP step W9.13, fleet phase 06. That step replaces this file wholesale
# and never edits the Makefile: the target `make lint-prose` already
# delegates here.
#
# ABSUMP_PLACEHOLDER. The existence of this file is not evidence that
# W9.13 has run. It prints one line and exits 0 so that
# `make -n lint-prose` and `make lint-prose` both work today.
echo "lint-prose: not built yet in this phase (owner: SOP W9.13, fleet phase 06)."
exit 0
