#!/bin/sh
# ops/b2_pull.sh -- PLACEHOLDER written by SOP step W1.14 (the Makefile).
#
# Owner: SOP step W1.10, a later fleet phase. That step replaces this file wholesale
# and never edits the Makefile: the target `make b2-pull` already
# delegates here.
#
# ABSUMP_PLACEHOLDER. The existence of this file is not evidence that
# W1.10 has run. It prints one line and exits 0 so that
# `make -n b2-pull` and `make b2-pull` both work today.
echo "b2-pull: not built yet in this phase (owner: SOP W1.10, a later fleet phase)."
exit 0
