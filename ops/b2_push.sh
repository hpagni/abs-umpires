#!/bin/sh
# ops/b2_push.sh -- PLACEHOLDER written by SOP step W1.14 (the Makefile).
#
# Owner: SOP step W1.10, a later fleet phase. That step replaces this file wholesale
# and never edits the Makefile: the target `make b2-push` already
# delegates here.
#
# ABSUMP_PLACEHOLDER. The existence of this file is not evidence that
# W1.10 has run. It prints one line and exits 0 so that
# `make -n b2-push` and `make b2-push` both work today.
echo "b2-push: not built yet in this phase (owner: SOP W1.10, a later fleet phase)."
exit 0
