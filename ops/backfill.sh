#!/bin/sh
# ops/backfill.sh -- PLACEHOLDER written by SOP step W1.14 (the Makefile).
#
# Owner: SOP step W6.0, fleet phase 02. That step replaces this file wholesale
# and never edits the Makefile: the target `make backfill` already
# delegates here.
#
# ABSUMP_PLACEHOLDER. The existence of this file is not evidence that
# W6.0 has run. It prints one line and exits 0 so that
# `make -n backfill` and `make backfill` both work today.
echo "backfill: not built yet in this phase (owner: SOP W6.0, fleet phase 02)."
exit 0
