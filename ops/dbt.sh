#!/bin/sh
# ops/dbt.sh -- PLACEHOLDER written by SOP step W1.14 (the Makefile).
#
# Owner: SOP step W1.11, fleet phase 01. That step replaces this file wholesale
# and never edits the Makefile: the target `make dbt` already
# delegates here.
#
# ABSUMP_PLACEHOLDER. The existence of this file is not evidence that
# W1.11 has run. It prints one line and exits 0 so that
# `make -n dbt` and `make dbt` both work today.
echo "dbt: not built yet in this phase (owner: SOP W1.11, fleet phase 01)."
exit 0
